name: Build Release

on:
  push:
    branches: [main]
  release:
    types: [created]

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install PyInstaller
        run: pip install pyinstaller

      - name: Generate resources
        run: python3 gen_resources.py

      - name: Build binary
        run: >
          pyinstaller
          --onefile
          --name predator-power
          --hidden-import curses
          --hidden-import predator_pkg
          --hidden-import predator_pkg.resources
          --collect-all predator_pkg
          predator-power.py

      - name: Upload artifact (main branch)
        if: github.event_name == 'push'
        uses: actions/upload-artifact@v4
        with:
          name: predator-power
          path: dist/predator-power

      - name: Upload to Release
        if: github.event_name == 'release'
        uses: softprops/action-gh-release@v2
        with:
          files: dist/predator-power
